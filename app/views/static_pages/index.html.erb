<h1 class="text-center">Welcome to the Bird's App</h1>
<div class="container">
  <section class="chirps">
    <% @chirps.each do |ch| %>
    <article class="chirp">
      <ul>
        <li>
          <%= ch.user.first_name %>
        </li>
      </ul>
      <%= ch.body %>
    </article>
    <% end %>
  </section>
</div>
